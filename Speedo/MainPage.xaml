<phone:PhoneApplicationPage x:Class="Speedo.MainPage"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:bar="clr-namespace:BindableApplicationBar;assembly=BindableApplicationBar"
                            xmlns:my="clr-namespace:Speedo"
                            xmlns:myControls="clr-namespace:Speedo.Controls"
                            xmlns:maps="clr-namespace:Microsoft.Phone.Maps.Controls;assembly=Microsoft.Phone.Maps"
                            xmlns:tool="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit" 
                            FontFamily="{StaticResource PhoneFontFamilyNormal}"
                            FontSize="{StaticResource PhoneFontSizeNormal}"
                            Foreground="{StaticResource PhoneForegroundBrush}"
                            SupportedOrientations="PortraitOrLandscape"
                            Orientation="Portrait"
                            shell:SystemTray.IsVisible="True">
    <shell:SystemTray.ProgressIndicator>
        <shell:ProgressIndicator IsIndeterminate="True" 
                                 IsVisible="{Binding IsLocating}" 
                                 Text="Locating..." />
    </shell:SystemTray.ProgressIndicator>
    <Grid>
        <Grid Background="{StaticResource PhoneBackgroundBrush}">
            <!-- TODO: LoadingError isn't supported any more, fix it? -->
            <myControls:BackgroundMap Source="{Binding MovementSource}" />

            <Grid RenderTransformOrigin="0.5,0.5">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Margin="10,0,10,0" VerticalAlignment="Bottom">
                    <TextBlock HorizontalAlignment="Center" 
                               VerticalAlignment="Top" 
                               Margin="0,0,0,20" 
                               Text="{Binding GpsStatus, Converter={StaticResource GpsToString}}" />

                    <myControls:SpeedDisplay Source="{Binding MovementSource}"
                                             Unit="{Binding Settings.SpeedUnit}" />
                </Grid>

                <Grid Margin="10,0,10,150" 
                      Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="64" />
                    </Grid.RowDefinitions>

                    <myControls:SpeedGraph Source="{Binding MovementSource}"
                                           PointsCount="360"
                                           Height="30"
                                           Visibility="{Binding ShowSpeedGraph, Converter={StaticResource BoolToVisibility}}" />

                    <myControls:DirectionDisplay Grid.Row="1"
                                                 Source="{Binding MovementSource}"
                                                 HorizontalAlignment="Center" />
                </Grid>

                <myControls:DistanceDisplay Grid.Row="2"
                                            HorizontalAlignment="Center"
                                            Margin="0,15"
                                            Source="{Binding MovementSource}" />
                <Grid.RenderTransform>
                    <ScaleTransform x:Name="ContentScaleTransform" />
                </Grid.RenderTransform>
            </Grid>

            <myControls:PhantomGrid>
                <Grid x:Name="windscreenRoot" 
                      HorizontalAlignment="Left" 
                      VerticalAlignment="Top">
                    <myControls:BubbleButton Command="{Binding SwitchWindscreenModeCommand}"
                                             Style="{StaticResource RoundIconButton}" 
                                             tool:TiltEffect.IsTiltEnabled="True">
                        <ImageBrush ImageSource="Resources/windscreen.png" />
                    </myControls:BubbleButton>
                    <Grid Visibility="{Binding IsWindscreenModeEnabled, Converter={StaticResource BoolToVisibility}}"
                          IsHitTestVisible="False" 
                          Background="{StaticResource PhoneAccentBrush}" 
                          Width="48" Height="48" 
                          Margin="16">
                        <Grid.OpacityMask>
                            <ImageBrush ImageSource="Resources/windscreen.png"/>
                        </Grid.OpacityMask>
                    </Grid>
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Grid.RenderTransform>
                </Grid>
                <Grid x:Name="mapRoot" 
                      HorizontalAlignment="Right" 
                      VerticalAlignment="Top">
                    <myControls:BubbleButton Command="{Binding SwitchMapStatusCommand}"
                                             Style="{StaticResource RoundIconButton}" 
                                             tool:TiltEffect.IsTiltEnabled="True">
                        <ImageBrush ImageSource="Resources/map.png" />
                    </myControls:BubbleButton>
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Grid.RenderTransform>
                </Grid>
                <Grid x:Name="alertRoot" 
                      HorizontalAlignment="Left" 
                      VerticalAlignment="Bottom">
                    <myControls:BubbleButton Command="{Binding SwitchSpeedAlertCommand}"
                                             Style="{StaticResource RoundIconButton}" 
                                             tool:TiltEffect.IsTiltEnabled="True">
                        <ImageBrush ImageSource="Resources/alert.png" />
                    </myControls:BubbleButton>
                    <Grid IsHitTestVisible="False" 
                          Visibility="{Binding SpeedAlert.IsEnabled, Converter={StaticResource BoolToVisibility}}"
                          Background="{StaticResource PhoneAccentBrush}" 
                          Width="48" Height="48" 
                          Margin="16">
                        <Grid.OpacityMask>
                            <ImageBrush ImageSource="Resources/alert.png"/>
                        </Grid.OpacityMask>
                    </Grid>
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Grid.RenderTransform>
                </Grid>
            </myControls:PhantomGrid>
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="OrientationStates">
                <VisualState x:Name="PortraitUp" />
                <VisualState x:Name="LandscapeRight">
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="alertRoot" 
                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" 
                                         To="80" 
                                         Duration="0" />
                        <DoubleAnimation Storyboard.TargetName="windscreenRoot" 
                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" 
                                         To="80" 
                                         Duration="0" />
                    </Storyboard>
                </VisualState>
                <VisualState x:Name="LandscapeLeft">
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="mapRoot" 
                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" 
                                         To="-80" 
                                         Duration="0" />
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <tool:TransitionService.NavigationInTransition>
            <tool:NavigationInTransition>
                <tool:NavigationInTransition.Backward>
                    <tool:TurnstileTransition Mode="BackwardIn"/>
                </tool:NavigationInTransition.Backward>
                <tool:NavigationInTransition.Forward>
                    <tool:TurnstileTransition Mode="ForwardIn"/>
                </tool:NavigationInTransition.Forward>
            </tool:NavigationInTransition>
        </tool:TransitionService.NavigationInTransition>
        <tool:TransitionService.NavigationOutTransition>
            <tool:NavigationOutTransition>
                <tool:NavigationOutTransition.Backward>
                    <tool:TurnstileTransition Mode="BackwardOut"/>
                </tool:NavigationOutTransition.Backward>
                <tool:NavigationOutTransition.Forward>
                    <tool:TurnstileTransition Mode="ForwardOut"/>
                </tool:NavigationOutTransition.Forward>
            </tool:NavigationOutTransition>
        </tool:TransitionService.NavigationOutTransition>
    </Grid>

    <bar:Bindable.ApplicationBar>
        <bar:BindableApplicationBar IsVisible="True"
                                    IsMenuEnabled="True"
                                    Mode="Minimized"
                                    Opacity="{Binding IsMenuVisible, RelativeSource={RelativeSource Self}, 
                                                      Converter={StaticResource BoolToAppBarOpacity}}">
            <bar:BindableApplicationBar.MenuItems>
                <bar:BindableApplicationBarMenuItem Text="{Binding Settings.SpeedUnit, Converter={StaticResource SpeedToSwitchString}}"
                                                    Command="{Binding SwitchUnitsCommand}" />
                <bar:BindableApplicationBarMenuItem Text="{Binding Settings.AllowLocationAccess, Converter={StaticResource LocationAccessToMenuString}}" 
                                                    Command="{Binding SwitchLocationAccessCommand}" />
                <bar:BindableApplicationBarMenuItem Text="about" 
                                                    Command="{Binding AboutCommand}" />
            </bar:BindableApplicationBar.MenuItems>
        </bar:BindableApplicationBar>
    </bar:Bindable.ApplicationBar>
</phone:PhoneApplicationPage>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       